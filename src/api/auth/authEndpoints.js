const authEndpoints = {
    login: '/auth/login',
    register: '/auth/register',
    logout: '/auth/logout',
    // Otros endpoints relacionados con la autenticación
  };
  
  export default authEndpoints;
  